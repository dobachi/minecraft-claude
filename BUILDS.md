# 建築記録

このワールドで建てたものの記録。座標と構成を残しておき、後のセッションから参照できるようにする。

## ワールド識別について

socket-be が返すワールド名は `World #0` で、これは**接続順に振られる内部ラベル**であり実際のワールド名ではない。同じ端末で別のワールドに繋いでも同じ名前になりうるので、ワールドの同定にはこの名前ではなく、**建築物の座標とシード的な地形の一致**を使うこと。

## 猫の家（3階建て）

- 記録日: 2026-09-05（ゲーム内 27日目・昼）
- 建築日: 同日（ゲーム内 24日目の朝に着工）
- 依頼者: SeaGosling63244

### 位置

| 項目 | 値 |
|---|---|
| 外周 | X133〜137 / Z-139〜-135（5×5） |
| 高さ | Y127（床）〜 Y139（屋根） |
| 建てた場所の地表 | Y127 の草ブロック（4隅とも確認済み・平坦） |

近くのプレイヤー初期位置は X130.7 / Z-139.2 付近。家はそこから見て東側。

### 構成

| 階 | 内部の高さ | 床 |
|---|---|---|
| 1F | Y128〜130 | Y127 |
| 2F | Y132〜134 | Y131 |
| 3F | Y136〜138 | Y135 |

- 素材: オークの板（`minecraft:oak_planks`）、外殻は `fill_area` の hollow
- 入口: 西壁中央 X133 Z-137 の Y128〜129。**木のドア**（Bedrock ID は `oak_door` ではなく `wooden_door`）
- 梯子: X136 Z-136 の Y128〜138 を一本で貫通。`ladder ["facing_direction"=4]`（東壁に接地）。Y131 と Y135 の床に穴を開けて連続させている
- 照明: 各階の天井中央 X135 Z-137 の Y131 / Y135 / Y139 にグロウストーン
- 窓: ガラス。1F は X137 Y129 Z-137 と X135 Y129 Z-135、2F・3F は東西壁の Y133 / Y137

### 3階の家具

| 位置 | ブロック |
|---|---|
| X134〜136 Y136 Z-138 | ソファ3つ（`sf_afm:sofa_0.block`） |
| X135 Y136 Z-137 | テーブル（`sf_afm:table_11.block`） |

真ん中のソファ（X135）はプレイヤーが手で設置したもの。両隣は `clone` で複製した。
**未確認**: 複製した2つと手で置いた1つで見た目が同じかどうか。違えば正しいIDで置き直す。

### 住人

野良猫を召喚し、プレイヤーが手なずけた。手なずけ済みなので despawn しない。

## 家具アドオン `sf_afm` の扱い

このワールドには家具アドオンが入っている。名前空間は `sf_afm`。

- **ID は `.block` で終わる**。`sf_afm:sofa_0.block` が正しく、`.block` を拡張子と誤解して削ると無効になる
- ソファは `sofa_0`、テーブルは `table_0` 〜 `table_11` まである
- ID の調べ方: ゲーム内チャットで `/setblock ~ ~ ~ sofa` のように途中まで打つと、名前空間込みの候補が一覧表示される
- 設置済みブロックの名前を知る方法: `testforblock <座標> air` の不一致メッセージに `tile.sf_afm:sofa_0.block.name` の形で出る。ここから `tile.` と `.name` を除いたものが ID
- ブロック状態は未特定。`direction` は「無効」と明示され、`minecraft:cardinal_direction` は north/south/east/west すべて不一致だった
- ID が分からない場合の回避策として `clone` が使える（ID を指定しないため、アドオンのブロックでも複製できる）
